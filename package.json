{
  "name": "functions",
  "version": "1.0.0",
  "description": "",
  "main": "functions/server.js",
  "scripts": {
    "start": "cd functions && node server.js",
    "watch": "cd functions && nodemon server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build"
  },
  "engines": {
    "node": "16.x"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.1.3",
    "dotenv": "^10.0.0",
    "express": "^4.17.2",
    "jquery": "^3.6.0",
    "mysql": "^2.12.0",
    "nodemon": "^2.0.15",
    "popper.js": "^1.16.1",
    "react-bootstrap-dropdown": "^0.3.0",
    "react-dropdown": "^1.9.2",
    "react-menu-list": "^7.0.3"
  }
}
